spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:testdb;DB_CLOSE_ON_EXIT=TRUE;MODE=PostgreSQL
    username: myuser
    password: mypassword
    hikari:
      auto-commit: false
  jooq:
    sql-dialect: H2
